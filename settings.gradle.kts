pluginManagement {
    repositories {
        google()
 import androidx.compose.foundation.layout.* 
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "PixGallery"
include(":app")
